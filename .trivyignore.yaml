# Trivy findings accepted for this repository. Each entry is explained in
# security/allowlist.md. Pass with: trivy ... --ignorefile .trivyignore.yaml

misconfigurations:
  - id: DS-0002
    paths:
      - "monitoring/datadog/Dockerfile"
    statement: "The Datadog agent must run as root to read the Docker socket, /proc and cgroups."
